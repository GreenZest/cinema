extends ./layout
block styles
  link(rel='stylesheet', href='/styles/403.css')
block body
  .page
    .main
      //- h1
      //-   | Forbidden
      h2 
        if typeof(message) !== 'undefined'
          = message
        else
          | You don't have permission to see the page you're trying to reach.
      //- p.help
      //-   a(href='http://en.wikipedia.org/wiki/HTTP_403') Why might this be happening?

